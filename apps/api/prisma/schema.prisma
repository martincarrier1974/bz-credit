generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:NewpDDAMUPWEkCgpdwlyhpGorVtJKwyH@postgres.railway.internal:5432/railway")
}

model Employee {
  id           String       @id @default(cuid())
  name         String
  expenses     Expense[]
  creditCards  CreditCard[]
}

model CreditCard {
  id               String   @id @default(cuid())
  name             String
  cardNumber       String?
  type             String?
  expirationMonth  String?
  expirationYear   String?
  active           Boolean  @default(true)
  csv              String?
  email            String?
  description      String?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  employeeId       String
  employee         Employee @relation(fields: [employeeId], references: [id], onDelete: Cascade)
}

model Supplier {
  id        String    @id @default(cuid())
  name      String
  expenses  Expense[]
}

model Category {
  id        String    @id @default(cuid())
  name      String
  expenses  Expense[]
}

model GlAccount {
  id        String    @id @default(cuid())
  code      String
  name      String
  expenses  Expense[]
}

model Expense {
  id          String    @id @default(cuid())
  title       String
  description String?
  date        String
  amount      Float
  tps         Float    @default(0)
  tvq         Float    @default(0)
  hasInvoice  Boolean  @default(false)
  validated   Boolean  @default(false)
  receiptPath String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  employeeId  String
  employee    Employee   @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  supplierId  String
  supplier    Supplier   @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  categoryId  String
  category    Category   @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  glAccountId String
  glAccount   GlAccount  @relation(fields: [glAccountId], references: [id], onDelete: Cascade)
}
